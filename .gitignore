# ============================================================
# product-data-toolkit .gitignore（根目录）
# 原则：源码 / 脚本 / 说明 / 空白模板 进库；
#       凭据、数据、运行产物、缓存、虚拟环境 一律挡在外面。
# 说明：根目录这一份对「本目录及所有子目录」生效。
# ============================================================

# ---------- 凭据（最高优先级：泄露即他人可顶替你登录）----------
session.json
last_run.json
token.txt
*.token
*.pat
.env
.env.*
# 明文口令文件：绝不进版本库，改用环境变量
*password*
*secret*

# ---------- Python 虚拟环境 ----------
.venv/
venv/
env/
.conda/

# ---------- Python 缓存 ----------
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.Python
pip-wheel-metadata/

# ---------- 日志 / 调试产物 ----------
*.log
failed/
_*.out
_*.err
_*.html

# ---------- 备份 / 缓存二进制 ----------
*.bak
*.bak2
*.pkl
.sap_cache/

# ---------- 空目录占位 ----------
.gitkeep

# ---------- 运行结果（可重新生成）----------
**/output/*
!**/output/.gitkeep

# ---------- 业务数据（体积大 + 含商业信息，绝不进库）----------
# 客户商品导入表
sku_matcher/imports/**/*.xlsx
sku_matcher/imports/*.xlsx
!sku_matcher/imports/商品导入模板*.xlsx
!sku_matcher/imports/**/商品导入模板*.xlsx
!sku_matcher/imports/商品导入模板*副本*.xlsx
# SAP 全量导出 / 数据源目录
# 规则按「失败即安全」写：目录里所有表格文件一律挡掉，只白名单放行模板。
# 这样以后换任何文件名的 SAP 导出（不必以 A006 开头）都自动被挡住，
# 不需要回来改这个文件。
sku_matcher/data_source/*.xlsx
sku_matcher/data_source/*.xls
sku_matcher/data_source/*.csv
!sku_matcher/data_source/*模板.xlsx
!sku_matcher/data_source/.gitkeep
# 待匹配原始货号清单
sku_matcher/input/**/*.xlsx
sku_matcher/input/*.xlsx
sku_matcher/input/*.xls
sku_matcher/input/*.csv
!sku_matcher/input/商品导入模板*.xlsx
# 分类树 / 待分类源表 / 主数据库
# 这三个目录全部是用户自备数据，直接整目录挡
product_classifier/data/*
product_classifier/input/*
product_classifier/master/*
!product_classifier/data/.gitkeep
!product_classifier/input/.gitkeep
!product_classifier/master/.gitkeep
# 上传成功后的备份台账
imported/**
# 登录态快照 / 浏览器抓取产物
upload_products_cn/legacy/calibrate/

# ---------- 编辑器 / 系统杂项 ----------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
